## Cursor Cloud specific instructions

This repository ("learning engine") is currently empty — it contains only a `README.md`. There are no source code files, dependencies, build systems, services, tests, or lint configurations.

When code is added to this repository, update the VM environment setup script (via `SetupVmEnvironment`) and this section with the relevant dependency installation commands, service startup instructions, and testing/lint/build notes.
